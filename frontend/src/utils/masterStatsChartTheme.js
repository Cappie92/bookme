/**
 * Линия «изменение %» на composed-графиках мастера (web) — зелёная, в одном стиле с Android/web-view.
 * Stacked bar: текущий период — подтверждённое / будущее (см. stackSegmentFills в MasterStats.jsx).
 */
export const MASTER_STATS_CHANGE_LINE_STROKE = '#4CAF50';
export const MASTER_STATS_CHANGE_LINE_DOT = '#4CAF50';

/** Подтверждённый сегмент stacked bar в текущем периоде — совпадает с «Факт» в сводке. */
export const MASTER_STATS_STACK_CONFIRMED_CURRENT = '#4CAF50';
/** Будущий сегмент (план) в текущем периоде — совпадает с «Факт + План» в скобках сводки. */
export const MASTER_STATS_STACK_PENDING_CURRENT = '#64B5F6';
