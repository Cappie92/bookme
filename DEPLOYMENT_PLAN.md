# План деплоя: Динамическое управление платными функциями в тарифах + Always Free план + исправления

## 📋 Обзор изменений

Добавлена возможность динамического управления платными функциями (service_functions) для каждого плана подписки через админку. Также добавлена централизация названий тарифных планов с возможностью редактирования. Исправлены проблемы с доступом к service_functions для мастеров и корректным отображением статуса подписки при заморозке. Реализован план Always Free как скрытая копия Premium для is_always_free пользователей. Настроены тестовые аккаунты с балансом и подписками.

---

## 🔧 Изменения в Backend

### 1. Модели (backend/models.py)

**Изменения:**
- Добавлено поле `display_name` в модель `SubscriptionPlan` (строка 1760)
- Добавлены поля `display_name` и `display_order` в модель `ServiceFunction` (строки 1801, 1804)

**Миграция:**
- Файл: `backend/alembic/versions/20250128_add_display_fields_to_plans_and_functions.py`
- Добавляет колонки `display_name` в `subscription_plans` и `service_functions`
- Добавляет колонку `display_order` в `service_functions`
- Заполняет существующие записи: `display_name = name`, `display_order = id`

### 2. Схемы (backend/schemas.py)

**Изменения:**
- Добавлено `display_name: Optional[str]` в `SubscriptionPlanBase`, `SubscriptionPlanCreate`, `SubscriptionPlanUpdate`, `SubscriptionPlanOut`
- Добавлены `display_name` и `display_order` в `ServiceFunctionBase`, `ServiceFunctionCreate`, `ServiceFunctionUpdate`, `ServiceFunctionOut`
- Добавлено `plan_display_name: Optional[str]` в `SubscriptionStatusOut`

### 3. Роутеры

#### backend/routers/subscription_plans.py
- Добавлена функция `validate_service_functions()` для валидации service_functions
- Добавлена функция `disable_service_functions_for_plan()` для автоматического отключения функций
- Обновлена функция `create_subscription_plan()` - добавлена валидация service_functions
- Обновлена функция `update_subscription_plan()` - добавлена проверка удаленных функций и их автоматическое отключение
- Добавлена поддержка `display_name` при создании/обновлении планов

#### backend/routers/service_functions.py
- Обновлена сортировка: `order_by(ServiceFunction.display_order, ServiceFunction.id)`

#### backend/routers/subscriptions.py
- Обновлен эндпоинт `/api/subscriptions/my` - добавлен `plan_display_name` в ответ

#### backend/routers/balance.py
- Обновлен эндпоинт `/api/balance/subscription-status` - добавлен `plan_display_name` в ответ

#### backend/routers/master.py
- Обновлен эндпоинт `/api/master/dashboard/stats` - добавлен `plan_display_name` в `subscription_info`

#### backend/utils/balance_utils.py
- Обновлена функция `get_subscription_status()` - добавлен `plan_display_name` в возвращаемый словарь
- Обновлена логика для is_always_free пользователей: создается реальная подписка на план AlwaysFree вместо виртуального статуса
- Добавлен импорт `SubscriptionStatus` и `SubscriptionType` для создания подписок

#### backend/utils/subscription_features.py
- Обновлена функция `get_user_subscription_with_plan()` - создает подписку на план AlwaysFree для is_always_free пользователей
- Обновлена функция `get_master_features()` - использует функции из реального плана подписки (включая AlwaysFree) вместо виртуальных максимальных функций

#### backend/routers/subscription_plans_public.py
- Обновлен эндпоинт `/api/subscription-plans/available` - добавлен фильтр для скрытия плана AlwaysFree из публичных списков

---

## 🎨 Изменения в Frontend

### 1. Компоненты

#### frontend/src/components/SubscriptionPlanForm.jsx
- Добавлено поле `display_name` в форму
- Добавлена загрузка service_functions при монтировании компонента
- **Объединены секции "Функции плана" и "Платные услуги" в одну секцию "Функции тарифа"**
- Удалены жестко закодированные чекбоксы (can_customize_domain, can_add_page_modules, has_finance_access, has_extended_stats)
- Все функции теперь загружаются динамически из service_functions
- **Список функций отображается в два столбца (grid grid-cols-2) вместо скролла**
- **Секция "Дополнительные настройки" показывается только если есть хотя бы одна настройка (модули или статистика)**
- **Удалено неиспользуемое поле `bookings_per_month` ("Записей в месяц") из формы редактирования плана**
- **Добавлена возможность установить бесконечное хранение статистики: значение 0 или пустое поле означает бесконечное хранение данных**
- Добавлены дополнительные настройки для функций с параметрами (max_page_modules, stats_retention_days)
- Сохранение выбранных service_functions в `features.service_functions` как массив ID

#### frontend/src/pages/MasterTariff.jsx
- Добавлена загрузка service_functions при монтировании компонента
- Обновлен вызов `getPlanFeatures()` с передачей `serviceFunctions`
- Обновлено использование названий планов через `getPlanDisplayName()`
- Упрощена логика отображения статуса подписки - используется единый источник `plan_display_name` из API
- Убрана логика "Платная (Название плана)" для is_always_free - теперь показывается только `plan_display_name`

#### frontend/src/components/SubscriptionModal.jsx
- Заменена локальная функция `getPlanName()` на `getPlanDisplayName()` из утилиты
- Добавлен фильтр для скрытия плана AlwaysFree из списка доступных планов для покупки

#### frontend/src/components/MasterDashboardStats.jsx
- Заменена локальная функция `getPlanNameInRussian()` на `getPlanDisplayName()` из утилиты

#### frontend/src/pages/MasterDashboard.jsx
- Обновлено отображение названия плана - используется `plan_display_name` из ответа API
- Убрана логика "Платная (Always Free)" - теперь используется только `plan_display_name` из API
- Упрощена логика отображения статуса подписки - используется единый источник данных

#### frontend/src/pages/MasterSubscriptionPlans.jsx
- Обновлено отображение названий планов через `getPlanDisplayName()`

#### frontend/src/pages/AdminFunctions.jsx
- **Добавлена загрузка service_functions для отображения в таблице планов**
- **Обновлено отображение функций в таблице планов: теперь используются service_functions из плана, а не старые поля features**
- **После сохранения плана список планов автоматически обновляется**

#### frontend/src/pages/MasterTariff.jsx
- **Исправлена загрузка service_functions: используется публичный эндпоинт `/api/master/service-functions`**
- **Обновлено отображение статуса подписки: если подписка заморожена или не может продолжаться, показывается "Приостановлена (Название плана)"**

### 2. Утилиты

#### frontend/src/utils/subscriptionPlanNames.js (новый файл)
- Создана централизованная утилита `getPlanDisplayName()` для получения отображаемых названий планов

#### frontend/src/utils/subscriptionFeatures.js
- Обновлена функция `getPlanFeatures()` - добавлен параметр `serviceFunctions`
- Добавлена логика объединения функций из конфигурации и service_functions
- Сортировка: сначала входящие в план, потом не входящие, внутри групп - по `display_order`
- Убрана логика "все функции доступны для is_always_free" - теперь используются функции из реального плана подписки

---

## 📦 Ранее внесенные исправления (включены в деплой)

### 1. Исправление ошибки 404 на `/api/subscriptions/freeze`
**Файл:** `backend/routers/subscriptions.py:463-525`
- Эндпоинт теперь возвращает `SubscriptionFreezeInfo` с дефолтными значениями вместо 404, если нет активной подписки
- Добавлена проверка `end_date > datetime.utcnow()`
- Возвращает `can_freeze: False`, `available_freeze_days: 0` если подписки нет

### 2. Исправление отображения условий тарифа
**Файл:** `backend/bookme.db` (план Pro)
- Обновлено поле `has_extended_stats` с `false` на `true` в JSON features плана Pro
**Файл:** `backend/schemas.py:931-947`
- Добавлены поля `features: Optional[Dict[str, Any]] = {}` и `limits: Optional[Dict[str, Any]] = {}` в схему `SubscriptionOut`

### 3. Исправление проверки подписки в дашборде
**Файл:** `backend/routers/master.py:2630-2670`
- Добавлена проверка `status == SubscriptionStatus.ACTIVE` и `end_date > datetime.utcnow()`
- Добавлен флаг `is_frozen: False` в `subscription_info` для отображения статуса заморозки
- При наличии активной заморозки `is_active` устанавливается в `False`, `is_frozen` в `True`

---

## 🚀 Порядок деплоя (без наката с нуля)

### Шаг 1: Применение миграции БД

```bash
cd backend
python -m alembic upgrade head
```

**Миграция:** `20250128_add_display_fields_to_plans_and_functions.py`
- Добавляет `display_name` в `subscription_plans`
- Добавляет `display_name` и `display_order` в `service_functions`
- Заполняет существующие записи значениями по умолчанию

### Шаг 2: Проверка данных после миграции

```bash
# Проверка subscription_plans
sqlite3 backend/bookme.db "SELECT id, name, display_name FROM subscription_plans;"

# Проверка service_functions
sqlite3 backend/bookme.db "SELECT id, name, display_name, display_order FROM service_functions;"
```

### Шаг 3: Создание плана Always Free

```bash
cd backend
python scripts/create_always_free_plan.py
```

**Скрипт:** `backend/scripts/create_always_free_plan.py`
- Создает план "AlwaysFree" как копию Premium
- Устанавливает `is_active = False` (скрыт от покупки)
- Устанавливает `display_order = 999` (не отображается в обычных списках)
- Копирует все `features` и `limits` из Premium

**Проверка:**
```bash
sqlite3 backend/bookme.db "SELECT id, name, display_name, is_active FROM subscription_plans WHERE name = 'AlwaysFree';"
```

### Шаг 4: Настройка тестовых аккаунтов

```bash
cd backend
python scripts/setup_test_accounts.py
```

**Скрипт:** `backend/scripts/setup_test_accounts.py`
- Зачисляет баланс 1 000 000 рублей на каждый тестовый аккаунт
- Создает реальные подписки для указанных тарифов:
  - +79435774916 -> Premium
  - +79435774941 -> Free
  - +79435774942 -> Basic
  - +79435774943 -> Pro
  - +79435774911 -> Free
  - +79435774912 -> Basic
  - +79435774913 -> Pro
  - +79435774914 -> Premium
  - +79435774944 -> Premium

**Проверка:**
```bash
sqlite3 backend/bookme.db "SELECT u.phone, sp.name, s.status FROM users u JOIN subscriptions s ON u.id = s.user_id JOIN subscription_plans sp ON s.plan_id = sp.id WHERE u.phone IN ('+79435774916', '+79435774941', '+79435774942', '+79435774943', '+79435774911', '+79435774912', '+79435774913', '+79435774914', '+79435774944');"
```

### Шаг 5: Создание недостающих service_functions

Создать service_functions для функций, которые раньше были в features:

```sql
INSERT INTO service_functions (name, display_name, description, function_type, is_active, display_order) VALUES 
('Редактирование названия домена', 'Редактирование названия домена', 'Возможность настройки собственного префикса сайта', 'SUBSCRIPTION', 1, 1),
('Модули страницы', 'Модули страницы', 'Добавление дополнительных модулей на персональную страницу мастера', 'SUBSCRIPTION', 1, 2),
('Финансы', 'Финансы', 'Доступ к финансовому учету и отчетности', 'SUBSCRIPTION', 1, 3),
('Расширенная статистика', 'Расширенная статистика', 'Расширенная аналитика и статистика по записям', 'SUBSCRIPTION', 1, 4),
('Стоп-листы и предоплата', 'Стоп-листы и предоплата', 'Управление стоп-листами клиентов и настройка обязательной предоплаты', 'SUBSCRIPTION', 1, 5)
ON CONFLICT DO NOTHING;
```

**Важно:** Эти функции уже должны быть созданы в БД. Если их нет, выполните SQL выше.

### Шаг 6: Удаление нереализованных service_functions (опционально)

Если нужно удалить нереализованные функции:

```sql
-- Удалить "Уведомления по SMS" (не реализовано как платная услуга)
UPDATE service_functions SET function_type = 'FREE', is_active = 0 WHERE name = 'Уведомления по SMS';

-- Удалить "Интеграция с календарем" (не реализовано)
UPDATE service_functions SET is_active = 0 WHERE name = 'Интеграция с календарем';
```

### Шаг 7: Обновление существующих планов (опционально)

Если нужно обновить `display_name` для существующих планов:

```sql
UPDATE subscription_plans SET display_name = 'Бесплатный' WHERE name = 'Free';
UPDATE subscription_plans SET display_name = 'Базовый' WHERE name = 'Basic';
UPDATE subscription_plans SET display_name = 'Профессиональный' WHERE name = 'Pro';
UPDATE subscription_plans SET display_name = 'Премиум' WHERE name = 'Premium';
```

**Важно:** После миграции все планы уже имеют `display_name = name`, так что этот шаг опционален.

### Шаг 8: Перезапуск Backend

```bash
# Если используется uvicorn с --reload, перезапуск произойдет автоматически
# Иначе перезапустите сервер вручную
```

### Шаг 9: Перезапуск Frontend

```bash
cd frontend
npm run build  # или npm run dev для разработки
```

### Шаг 10: Очистка кэша браузера

Рекомендуется очистить кэш браузера или выполнить жесткую перезагрузку (Ctrl+Shift+R / Cmd+Shift+R)

---

## ✅ Проверка после деплоя

### 1. Проверка Backend

1. **Проверка миграции:**
   ```bash
   sqlite3 backend/bookme.db "PRAGMA table_info(subscription_plans);" | grep display_name
   sqlite3 backend/bookme.db "PRAGMA table_info(service_functions);" | grep -E "display_name|display_order"
   ```

2. **Проверка API:**
   - `GET /api/admin/subscription-plans` - должен возвращать `display_name`, план AlwaysFree должен быть в списке
   - `GET /api/subscription-plans/available?subscription_type=master` - план AlwaysFree НЕ должен быть в списке
   - `GET /api/admin/service-functions` - должен возвращать `display_name` и `display_order`
   - `GET /api/subscriptions/my` - должен возвращать `plan_display_name`
   - `GET /api/balance/subscription-status` - должен возвращать `plan_display_name`
   - `GET /api/master/service-functions` - должен возвращать список service_functions для мастеров

### 2. Проверка Frontend

1. **Админка:**
   - Открыть `/admin/functions` → вкладка "Планы подписки"
   - Создать/редактировать план - проверить наличие поля "Отображаемое название"
   - **Проверить, что есть только одна секция "Функции тарифа" (без разделения на "Функции плана" и "Платные услуги")**
   - Проверить, что все функции (включая бывшие "Функции плана") отображаются в едином списке
   - Выбрать несколько service_functions и сохранить план
   - Проверить, что выбранные функции сохранились
   - Проверить дополнительные настройки (max_page_modules, stats_retention_days) - должны появляться при выборе соответствующих функций

2. **Страница "Мой тариф":**
   - Войти как мастер с подпиской
   - Открыть `/master/tariff`
   - Проверить отображение всех service_functions с отметками ✅/❌
   - Проверить, что для Free плана всегда показывается "Ограничение в 30 активных записей"
   - Проверить правильный порядок: сначала входящие функции, потом не входящие

3. **Названия планов:**
   - Проверить, что везде используются `display_name` вместо хардкодных названий
   - Проверить страницу выбора тарифа
   - Проверить дашборд мастера
   - Проверить страницу "Мой тариф"

4. **План Always Free:**
   - Проверить, что план AlwaysFree создан и скрыт от покупки (`is_active = False`)
   - Проверить, что план AlwaysFree не отображается в модальном окне выбора тарифа
   - Проверить, что для is_always_free пользователей создается подписка на план AlwaysFree
   - Проверить, что функции из плана AlwaysFree корректно применяются к is_always_free пользователям

5. **Тестовые аккаунты:**
   - Проверить баланс тестовых аккаунтов (должен быть 1 000 000 рублей)
   - Проверить подписки тестовых аккаунтов (должны соответствовать указанным тарифам)

---

## 🔄 Откат изменений (если нужно)

### Откат миграции:

```bash
cd backend
python -m alembic downgrade -1
```

Это удалит добавленные колонки `display_name` и `display_order`.

### Откат кода:

```bash
git checkout HEAD~1  # или конкретный коммит до изменений
```

---

## 📝 Дополнительные заметки

1. **Обратная совместимость:** Все изменения обратно совместимы. Если `display_name` пустое, используется `name`.

2. **Хранение service_functions:** Связь между планами и service_functions хранится в JSON поле `features.service_functions` как массив ID: `[2, 4, 5, 7, 8]`

3. **Автоматическое отключение:** При удалении service_function из плана она автоматически становится недоступной для всех мастеров с этим планом при следующей проверке доступа.

4. **Сортировка:** Service_functions сортируются по `display_order`, затем по `id`. При отображении - сначала входящие в план, потом не входящие.

5. **Объединение секций:** Секции "Функции плана" и "Платные услуги" объединены в одну секцию "Функции тарифа". Все функции (включая бывшие can_customize_domain, can_add_page_modules, has_finance_access, has_extended_stats) теперь управляются через service_functions.

6. **Миграция старых функций:** Функции, которые раньше были в features (can_customize_domain и т.д.), теперь созданы как service_functions в БД и должны быть добавлены в планы через админку.

7. **Лимиты записей:** В форме плана есть два поля для лимитов:
   - `bookings_per_month` ("Записей в месяц") - **НЕ используется** в коде, можно удалить в будущем
   - `max_future_bookings` ("Максимум активных записей") - **активно используется** для проверки лимита одновременно активных будущих записей

8. **UI улучшения:**
   - Список функций тарифа отображается в два столбца (grid grid-cols-2) вместо скролла
   - Секция "Дополнительные настройки" показывается только если есть хотя бы одна настройка
   - Список функций в таблице планов обновляется автоматически после сохранения изменений

9. **План Always Free:**
   - План AlwaysFree является скрытой копией Premium (`is_active = False`)
   - Не отображается в публичных списках планов для покупки
   - Используется только для is_always_free пользователей
   - Создается автоматически при первом обращении к функциям для is_always_free пользователей
   - Все функции из плана AlwaysFree идентичны Premium

10. **Тестовые аккаунты:**
    - Все тестовые аккаунты имеют баланс 1 000 000 рублей
    - Для каждого аккаунта создана реальная подписка на указанный тариф
    - Аккаунты готовы для тестирования в максимально боевом режиме

11. **Единый источник данных для названий тарифов:**
    - Во всех местах отображения тарифа используется `plan_display_name` из API (`/api/balance/subscription-status`)
    - Убрана логика добавления префиксов "Платная (Always Free)" или "Платная (Название плана)"
    - Название тарифа берется напрямую из `plan_display_name` плана подписки
    - Для замороженных подписок показывается "Приостановлена (Название плана)"

---

## 🐛 Известные проблемы и ограничения

Нет известных проблем на момент создания плана деплоя.

---

## 🧪 Базовые тесты

### Тест 1: Проверка миграции
```bash
# Проверка наличия колонок
sqlite3 backend/bookme.db "PRAGMA table_info(subscription_plans);" | grep display_name
sqlite3 backend/bookme.db "PRAGMA table_info(service_functions);" | grep -E "display_name|display_order"

# Проверка заполнения данных
sqlite3 backend/bookme.db "SELECT id, name, display_name FROM subscription_plans;"
sqlite3 backend/bookme.db "SELECT id, name, display_name, display_order FROM service_functions;"
```

### Тест 2: Проверка API эндпоинтов
```bash
# Получить планы (должен вернуть display_name)
curl -H "Authorization: Bearer <admin_token>" http://localhost:8000/api/admin/subscription-plans

# Получить service_functions (должен вернуть display_name и display_order)
curl -H "Authorization: Bearer <admin_token>" http://localhost:8000/api/admin/service-functions?function_type=subscription

# Получить подписку (должен вернуть plan_display_name)
curl -H "Authorization: Bearer <user_token>" http://localhost:8000/api/subscriptions/my
```

### Тест 3: Проверка валидации service_functions
- Создать план с несуществующим service_function ID - должна быть ошибка
- Создать план с неактивным service_function - должна быть ошибка
- Создать план с service_function типа FREE - должна быть ошибка
- Создать план с корректными service_functions - должно работать

### Тест 4: Проверка автоматического отключения
- Создать план с service_functions [2, 4, 5]
- Создать подписку на этот план для мастера
- Обновить план, убрав service_function 5
- Проверить, что функция 5 больше не доступна для мастера (при следующей проверке доступа)

---

## 📞 Контакты для вопросов

При возникновении проблем при деплое проверьте:
1. Логи backend (uvicorn)
2. Логи frontend (браузерная консоль)
3. Состояние БД после миграции
